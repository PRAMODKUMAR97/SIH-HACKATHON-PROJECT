# Demo mode requires no credentials and uses explicitly labelled bundled fixtures.
DEMO_MODE=true
# Data Mode searches a public Sentinel-2 STAC catalogue; no paid service is required.
# Keep API keys out of this file and do not commit a populated .env.
# EARTH_ENGINE_PROJECT=
# DATABASE_URL=sqlite:///./khanan_netra.db
