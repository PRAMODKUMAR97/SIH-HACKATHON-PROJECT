services:
  khanan-netra:
    image: python:3.11-slim
    working_dir: /app
    volumes: [".:/app"]
    ports: ["8000:8000"]
    command: sh -c "pip install --no-cache-dir -r requirements.txt && python run_demo.py"
